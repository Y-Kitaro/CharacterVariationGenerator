# Model Paths Configuration

models:
  mask_generator:
    segmentation_model_path: "assets/models/sam3.pt"
  expression_editor:
    model_directories:
      - "assets/models/sdxl"

device: "cuda"

processing:
  upscale_factor: 2
  default_denoising_strength: 0.50

face_detailer:
  guide_size: 1024
